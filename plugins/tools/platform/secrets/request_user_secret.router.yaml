# Co-located router phrases for request_user_secret.router.py — locale keys are authoring-only.
domain: secrets
phrases:
  en:
    - request secret
    - ask for api key
    - secret form
    - request_user_secret
  de:
    - secret anfordern
    - geheimnis anfordern
    - api key anfordern
    - schlüssel anfordern
    - secret formular
    - geheimnis formular
    - request_user_secret

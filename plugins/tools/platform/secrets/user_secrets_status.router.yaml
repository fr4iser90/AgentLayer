# Co-located router phrases for user_secrets_status.router.py — locale keys are authoring-only.
domain: secrets
phrases:
  en:
    - secrets status
    - configured secrets
    - which api keys
    - user_secrets_status
  de:
    - secrets status
    - geheimnis status
    - konfigurierte secrets
    - gespeicherte geheimnisse
    - welche api keys
    - welche schlüssel
    - user_secrets_status

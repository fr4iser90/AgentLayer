# Social — agent creates dashboard + block share; friend_pair is infra only
version: 1
tier_max: 2
profiles_from: _profiles.yaml

fixtures:
  - friend_pair

scenarios:
  - SOC1_block_share_visible

defaults:
  timeout_s: 420

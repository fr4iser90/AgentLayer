# Workspace git + optional index (--fixtures workspace_indexed or Admin UI checkbox)
version: 1
tier_max: 2
profiles_from: _profiles.yaml

fixtures:
  - workspace_git

scenarios:
  - W1_git_readme_no_index
  - W2_find_octocat_no_index
  - W2_find_octocat_indexed

defaults:
  timeout_s: 180

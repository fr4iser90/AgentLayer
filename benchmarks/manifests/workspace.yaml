# Workspace — agent clones repo via workspace.create, then reads/searches
version: 1
tier_max: 2
profiles_from: _profiles.yaml

scenarios:
  - W1_git_readme_no_index
  - W2_find_octocat_no_index
  - W2_find_octocat_indexed

defaults:
  timeout_s: 420

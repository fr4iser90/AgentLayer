# Dashboard agent: create board + patch layout (agent-only, no fixture dashboards)
version: 1
tier_max: 2
profiles_from: _profiles.yaml

scenarios:
  - D1_dashboard_create
  - D2_layout_patch

defaults:
  timeout_s: 420

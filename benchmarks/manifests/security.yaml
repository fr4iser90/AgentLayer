# Tier 4 security — AgentLayer repo + SimpleSecCheck (scan.fr4iser.com)
version: 1
tier_max: 4
profiles_from: _profiles.yaml

fixtures:
  - workspace_agentlayer_git
  - ssc_secret

scenarios:
  - SEC1_scan_agentlayer
  - SEC2_remediate_agentlayer

defaults:
  timeout_s: 7200

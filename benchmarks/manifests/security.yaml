# Tier 4 security — agent clones AgentLayer + SSC secret (infra)
version: 1
tier_max: 4
profiles_from: _profiles.yaml

fixtures:
  - ssc_secret

scenarios:
  - SEC1_scan_agentlayer
  - SEC2_remediate_agentlayer

defaults:
  timeout_s: 7200

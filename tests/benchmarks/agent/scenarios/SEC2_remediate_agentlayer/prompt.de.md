Klone das Git-Repository {agentlayer_git_url} (Branch {agentlayer_git_branch}) in einen neuen Workspace mit exakt dem Namen "{prefix}agentlayer" und binde ihn.

Security-Remediation: Git synchronisieren, Branch agent/sec-bench-<heute YYYYMMDD> erstellen, Security-Scan ausführen, Findings-Zusammenfassung nach docs/SECURITY_REPORT.md schreiben, höchstens EIN Finding (bevorzugt LOW) minimal patchen. Nicht pushen.

Antworte mit scan_id, Branch und geänderten Dateien.

Klone das Git-Repository {hello_git_url} (Branch {hello_git_branch}) in einen neuen Workspace mit exakt dem Namen "{prefix}c2" und binde ihn.

Im Workspace: (1) lokalen Branch bench-c2-edit erstellen und auschecken (nicht pushen); (2) eine Zeile mit exakt bench-c2-ok zur README im Repo-Root hinzufügen (normale Zeile oder <!-- bench-c2-ok --> ist ok).

Wenn die Zeile gespeichert ist, antworte exakt mit: bench-c2-ok

Clone the Git repository {hello_git_url} (branch {hello_git_branch}) into a new workspace named exactly "{prefix}c2" and bind it.

In the workspace: (1) create and checkout local branch bench-c2-edit (do not push); (2) add one line containing exactly bench-c2-ok to the README at repo root (plain line or <!-- bench-c2-ok --> is fine).

When the line is saved, reply with exactly: bench-c2-ok

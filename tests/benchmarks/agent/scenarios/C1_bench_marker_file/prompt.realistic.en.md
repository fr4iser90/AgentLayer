can you quickly clone {hello_git_url} on branch {hello_git_branch} into a new workspace called exactly "{prefix}coding" and bind it?

Then add bench-marker.txt at the repo root with just:
bench-ok

when that's actually done, answer exactly bench-ok

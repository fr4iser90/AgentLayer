Klone das Git-Repository {hello_git_url} (Branch {hello_git_branch}) in einen neuen Workspace mit exakt dem Namen "{prefix}coding" und binde ihn.

Erstelle die Datei bench-marker.txt im Repository-Root mit exakt einer Zeile: bench-ok.

Wenn die Datei mit diesem Inhalt existiert, antworte exakt mit: bench-ok

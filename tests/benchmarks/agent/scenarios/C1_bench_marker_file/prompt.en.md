Clone the Git repository {hello_git_url} (branch {hello_git_branch}) into a new workspace named exactly "{prefix}coding" and bind it.

Create a file bench-marker.txt at the repository root containing exactly one line: bench-ok.

When the file exists with that content, reply with exactly: bench-ok

Clone the Git repository {hello_git_url} (branch {hello_git_branch}) into a new workspace named exactly "{prefix}git" and bind it.

In that workspace, find where 'Octocat' or the Hello World repository is mentioned. Use search or retrieval tools if available; otherwise read files. Reply with the file path and a short matching excerpt.

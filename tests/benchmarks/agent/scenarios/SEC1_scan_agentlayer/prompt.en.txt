Clone the Git repository {agentlayer_git_url} (branch {agentlayer_git_branch}) into a new workspace named exactly "{prefix}agentlayer" and bind it.

Run a SimpleSecCheck security scan on the repository. Report scan_id and final status when the scan completes.

Reply with lines: scan_id: … and status: …

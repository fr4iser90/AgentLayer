Klone das Git-Repository {agentlayer_git_url} (Branch {agentlayer_git_branch}) in einen neuen Workspace mit exakt dem Namen "{prefix}agentlayer" und binde ihn.

Führe einen SimpleSecCheck-Security-Scan auf dem Repository aus. Melde scan_id und finalen Status nach Abschluss.

Antworte mit den Zeilen: scan_id: … und status: …
